apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: mongo-pvc
spec:
    storageClassName: aws-ssd-storage
    accessModes:
      - ReadWriteOnce
    resources:
       requests:
         storage: 5Gi